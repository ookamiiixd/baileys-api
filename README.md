# Wavvy
Wavvy is a self-hosted WhatsApp API gateway that utilizes the [Baileys](https://github.com/WhiskeySockets/Baileys) library into a clean REST API with built-in dashboard for managing your connections.
